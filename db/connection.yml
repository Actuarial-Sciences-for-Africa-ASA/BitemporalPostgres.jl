env: ENV["GENIE_ENV"]

dev:
  adapter: PostgreSQL
  database: postgres
  host: 127.0.0.1
  port: 5432
  config:
    log_queries: false
    log_level: Error

prod:
  adapter:
  database:
  host:
  port:
  config:

test:
  adapter: PostgreSQL
  database: postgres
  host: 127.0.0.1
  port: 5432
  config:
